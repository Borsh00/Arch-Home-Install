# Arch Home Install

Набор скриптов для локальной установки Arch Linux окружения **без sudo**  
(через `proot`, локальные бинарники и `$HOME`).

## Быстрая установка

Одна команда:

```bash
curl -fsSL https://raw.githubusercontent.com/Borsh00/Arch-Home-Install/main/install.sh | bash
